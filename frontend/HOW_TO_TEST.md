# 前端功能测试指南

## 测试前准备

### 1. 启动后端服务

```bash
cd backend
.\start.ps1
```

确保后端服务运行在 `http://localhost:8888`

### 2. 启动前端服务

```bash
cd frontend
pnpm dev
```

前端服务将运行在 `http://localhost:5173`

### 3. 准备测试账号

建议创建以下角色的测试账号：
- 学生账号（student）
- 教师账号（teacher）
- 主任账号（director）
- 院长账号（dean）
- 管理员账号（admin）

### 4. 准备测试文件

- 正常的 .docx 论文文件
- 大文件（接近 50MB）测试上传限制
- 非 .docx 文件测试格式验证

---

## 功能测试步骤

### 一、认证功能测试

#### 1. 注册功能
1. 访问 http://localhost:5173/register
2. 填写注册表单
3. 测试点：
   - 用户名/邮箱格式验证
   - 密码长度验证（6位以上）
   - 确认密码一致性检查
   - 注册成功后跳转到登录页

#### 2. 登录功能
1. 访问 http://localhost:5173/login
2. 输入用户名和密码
3. 测试点：
   - 登录成功跳转到首页
   - Token 存储到 localStorage
   - 错误提示显示
   - 自动登录（刷新页面保持登录状态）

#### 3. 退出登录
1. 点击顶部用户头像
2. 点击"退出登录"
3. 测试点：
   - 清除 Token
   - 跳转到登录页
   - 再次访问需要登录

---

### 二、首页测试（所有角色）

1. 登录后访问首页 `/`
2. 测试点：
   - 统计卡片显示正确数据
   - 最近论文列表显示（如有数据）
   - 快捷入口按钮可点击
   - 点击论文跳转到详情页

---

### 三、论文管理测试（学生+）

#### 1. 论文列表
1. 访问 `/papers`
2. 测试点：
   - 表格显示论文列表
   - 搜索功能（输入标题搜索）
   - 类型筛选（毕业论文/课程论文）
   - 分页功能
   - 操作按钮：查看详情、提交检查、删除

#### 2. 上传论文
1. 访问 `/papers/upload`
2. 测试点：
   - **文件上传**
     - 拖拽上传 .docx 文件
     - 点击上传选择文件
     - 文件格式验证（只允许 .docx）
     - 文件大小验证（最大 50MB）
   - **表单验证**
     - 论文标题必填
     - 论文类型必填
     - 根据类型显示不同字段
   - **毕业论文字段**
     - 专业、院系、指导老师、学年
   - **课程论文字段**
     - 课程名称、课程代码、学期、授课教师
   - 上传成功跳转到论文列表

#### 3. 论文详情
1. 从列表点击"详情"或访问 `/papers/:id`
2. 测试点：
   - 论文元数据正确显示
   - 检查历史表格（如有检查记录）
   - "提交检查"按钮跳转到检查提交页
   - "删除论文"按钮弹出确认框

---

### 四、检查功能测试（学生+）

#### 1. 提交检查
1. 从论文详情或列表点击"提交检查"
2. 访问 `/check/submit/:paperId`
3. 测试点：
   - 显示论文基本信息
   - **检查类型选择**
     - 至少选择一项
     - 可多选
     - 5种类型：标题/格式/内容/查重/逻辑
   - **模板选择**
     - 根据论文类型筛选（毕业/课程）
     - 显示公开和自己的私有模板
   - 提交成功跳转到检查状态页

#### 2. 检查状态（WebSocket 实时）
1. 提交检查后自动跳转到 `/check/status/:taskId`
2. 测试点：
   - **WebSocket 连接**
     - 页面顶部显示连接状态
     - 断开后自动重连
   - **进度显示**
     - Progress 进度条动画
     - 当前阶段提示（Spin 组件）
     - 百分比实时更新
   - **状态表格**
     - 显示状态、阶段、进度
   - **完成后**
     - 成功：显示成功 Result，5秒后跳转结果详情
     - 失败：显示失败 Result，显示错误信息

---

### 五、结果查看测试（学生+）

#### 1. 结果列表
1. 访问 `/results`
2. 测试点：
   - 表格显示检查结果
   - **成绩标签**
     - 优秀（绿色）
     - 良好（蓝色）
     - 中等（橙色）
     - 及格（默认色）
     - 不及格（红色）
   - **重复率颜色**
     - >30%: 红色
     - 15-30%: 橙色
     - <15%: 绿色
   - 搜索功能（论文标题）
   - 分页功能
   - 点击行跳转到详情页

#### 2. 结果详情
1. 从列表点击或访问 `/results/:id`
2. 测试点：
   - **统计卡片（4个）**
     - 总体成绩（带标签）
     - 重复率（带颜色）
     - 问题总数
     - 检查时间
   - **各项成绩**
     - 标题检查分数
     - 格式检查分数
     - 内容检查分数
     - 逻辑检查分数
   - **问题列表（4个标签页）**
     - 标题问题
     - 格式问题
     - 内容问题
     - 逻辑问题
     - 每个问题显示：位置、严重程度、描述、建议
     - 严重程度标签：严重（红）/警告（橙）/提示（蓝）

---

### 六、个人中心测试（所有角色）

1. 访问 `/profile`
2. 测试点：
   - **基本信息标签页**
     - 显示所有用户信息
     - 格式正确（日期格式化）
   - **编辑资料标签页**
     - 表单预填充当前信息
     - 可修改：昵称、电话、院系、专业
     - 保存成功提示
     - 刷新页面验证修改生效
   - **修改密码标签页**
     - 旧密码、新密码、确认密码
     - 确认密码验证
     - 修改成功提示
     - 需要重新登录

---

### 七、模板管理测试（教师+）

1. 使用教师/主任/院长/管理员账号登录
2. 访问 `/templates`
3. 测试点：
   - **模板列表**
     - 表格显示模板
     - 类型标签（毕业论文/课程论文）
     - 可见性标签（公开/私有）
   - **上传模板**
     - 点击"上传模板"按钮
     - 填写表单：名称、描述、类型、可见性
     - 上传文件
     - 上传成功
   - **编辑模板**
     - 点击"编辑"按钮
     - 只能修改名称、描述、类型、可见性
     - 不能修改文件
     - 保存成功
   - **删除模板**
     - 点击"删除"按钮
     - 弹出确认框
     - 确认后删除成功

---

### 八、用户管理测试（管理员）

1. 使用管理员账号登录
2. 访问 `/users`
3. 测试点：
   - **用户列表**
     - 表格显示用户
     - 角色标签颜色区分
   - **搜索功能**
     - 输入用户名或邮箱搜索
     - 搜索结果正确
   - **角色筛选**
     - 下拉选择角色
     - 筛选结果正确
   - **修改角色**
     - 点击"改角色"按钮
     - 弹出选择框
     - 选择新角色
     - 保存成功
     - 刷新验证修改生效
   - **删除用户**
     - 点击"删除"按钮
     - 弹出确认框
     - 确认后删除成功

---

### 九、参数设置测试（主任+）

#### 1. 主任账号测试
1. 使用主任账号登录
2. 访问 `/parameters`
3. 测试点：
   - **参数列表**
     - 表格显示参数
     - 适用范围标签（全局/院系/专业）
     - 状态标签（已锁定/未锁定）
   - **新增参数**
     - 点击"新增参数"按钮
     - 填写表单（所有阈值 0-100）
     - 创建成功
   - **编辑参数**
     - 未锁定参数可编辑
     - 已锁定参数不可编辑（按钮禁用）
   - **锁定操作**
     - 主任看不到锁定/解锁按钮

#### 2. 院长账号测试
1. 使用院长账号登录
2. 访问 `/parameters`
3. 测试点：
   - **锁定/解锁**
     - 可以看到锁定/解锁按钮
     - 点击切换锁定状态
     - 操作成功
   - **编辑锁定参数**
     - 院长可以编辑已锁定的参数
     - 主任不可编辑

---

### 十、往届论文管理测试（教师+）

1. 使用教师/主任/院长/管理员账号登录
2. 访问 `/previous-papers`
3. 测试点：
   - **论文列表**
     - 表格显示往届论文
     - 显示标题、作者、年份、院系、专业、上传时间
   - **搜索功能**
     - 输入标题或作者搜索
     - 搜索结果正确
   - **年份筛选**
     - 下拉选择年份
     - 筛选结果正确
   - **上传论文**
     - 点击"上传论文"按钮
     - 填写表单：标题、作者、年份、院系、专业
     - 上传 .docx 文件
     - 文件大小验证（50MB）
     - 上传成功
   - **删除论文**
     - 点击"删除"按钮
     - 弹出确认框
     - 确认后删除成功

---

### 十一、统计分析测试（主任+）

#### 1. 主任账号测试
1. 使用主任账号登录
2. 访问 `/statistics`
3. 测试点：
   - **日期范围选择**
     - 默认显示最近30天
     - 选择自定义日期范围
     - 数据更新
   - **总览卡片**
     - 论文总数、已完成数、检查中数、失败数
     - 数据正确
   - **检查状态分布（饼图）**
     - 饼图正确渲染
     - 图例显示
     - 鼠标悬停显示详情
   - **成绩分布（柱状图）**
     - 柱状图正确渲染
     - 不同成绩不同颜色
     - 鼠标悬停显示数量
   - **教师统计（柱状图）**
     - 显示 Top 10 教师检查次数
     - 主任可见
   - **院系对比**
     - 主任不可见此图表

#### 2. 院长账号测试
1. 使用院长账号登录
2. 访问 `/statistics`
3. 测试点：
   - 除主任权限外，还可见：
   - **院系对比（柱状图+折线图）**
     - 柱状图显示论文数
     - 折线图显示优秀率
     - 双 Y 轴正确
     - 院长独有

---

### 十二、权限控制测试

#### 1. 学生账号
- 可访问：首页、论文管理、检查、结果、个人中心
- 不可访问：模板、用户、参数、往届论文、统计
- 测试：尝试访问 `/templates`，应跳转 403 页面

#### 2. 教师账号
- 学生权限 + 模板管理、往届论文管理
- 不可访问：用户管理、参数设置、统计分析
- 测试：尝试访问 `/parameters`，应跳转 403 页面

#### 3. 主任账号
- 教师权限 + 参数设置、统计分析（教师级）
- 不可访问：用户管理、院系对比图表
- 测试：访问 `/statistics`，不应看到院系对比图

#### 4. 院长账号
- 主任权限 + 院系级统计数据、参数锁定解锁
- 不可访问：用户管理
- 测试：访问 `/statistics`，应看到院系对比图

#### 5. 管理员账号
- 所有权限 + 用户管理
- 可访问所有页面

---

### 十三、错误页面测试

#### 1. 404 页面
1. 访问不存在的路由，如 `/not-exist`
2. 测试点：
   - 显示 404 页面
   - "返回首页"按钮可用
   - 点击返回首页

#### 2. 403 页面
1. 使用学生账号访问 `/templates`
2. 测试点：
   - 自动跳转到 403 页面
   - 显示权限不足提示
   - "返回首页"按钮可用
   - 点击返回首页

---

### 十四、响应式布局测试

1. 调整浏览器窗口大小
2. 测试点：
   - 侧边栏自动折叠
   - 表格横向滚动
   - 移动端适配

---

### 十五、性能测试

1. **加载性能**
   - 首屏加载时间 < 3s
   - 路由懒加载生效（Network 面板查看）
   - 图片/图表按需加载

2. **交互性能**
   - 表格分页流畅
   - 搜索/筛选响应及时
   - WebSocket 实时更新无延迟

3. **网络请求**
   - 打开浏览器开发者工具 Network 面板
   - 检查 API 请求是否携带 Token
   - 检查 401 响应是否自动跳转登录
   - 检查请求失败是否有错误提示

---

## 测试清单

### 必测功能
- [ ] 登录/注册
- [ ] 论文上传（文件验证）
- [ ] 检查状态（WebSocket 实时）
- [ ] 结果详情（问题分类）
- [ ] 权限控制（不同角色）
- [ ] 统计分析（ECharts 图表）

### 重点测试
- [ ] WebSocket 断线重连
- [ ] 文件上传（大文件、格式验证）
- [ ] 权限路由拦截
- [ ] Token 过期处理
- [ ] 表单验证

### 边界测试
- [ ] 文件大小限制（50MB）
- [ ] 密码长度（最少6位）
- [ ] 权限等级（5级）
- [ ] 日期选择（范围验证）

---

## 常见问题排查

### 1. 无法登录
- 检查后端服务是否启动
- 检查 Network 面板是否有 CORS 错误
- 检查用户名和密码是否正确

### 2. Token 失效
- 清除浏览器 localStorage
- 重新登录

### 3. WebSocket 连接失败
- 检查后端 WebSocket 服务是否启动
- 检查控制台是否有连接错误
- 确认 WebSocket URL 正确

### 4. 文件上传失败
- 检查文件格式（.docx）
- 检查文件大小（<50MB）
- 检查 Network 面板查看请求

### 5. 权限不足
- 确认当前用户角色
- 确认页面所需权限等级
- 使用更高权限账号测试

---

## 测试报告模板

```markdown
# 前端功能测试报告

**测试日期**: YYYY-MM-DD  
**测试人员**: XXX  
**测试环境**: 
- 前端：http://localhost:5173
- 后端：http://localhost:8888
- 浏览器：Chrome/Firefox/Edge

## 测试结果

| 功能模块 | 测试项 | 预期结果 | 实际结果 | 状态 |
|---------|--------|---------|---------|------|
| 认证 | 登录 | 成功跳转首页 | 符合预期 | ✅ |
| 认证 | 注册 | 成功跳转登录 | 符合预期 | ✅ |
| ... | ... | ... | ... | ... |

## 发现的问题

1. **问题描述**: XXX
   - **严重程度**: 高/中/低
   - **复现步骤**: 1. 2. 3.
   - **期望行为**: XXX
   - **实际行为**: XXX

## 测试总结

- 测试用例总数：XX
- 通过数：XX
- 失败数：XX
- 通过率：XX%

## 建议

1. ...
2. ...
```

---

## 自动化测试建议

后续可以添加：
1. **单元测试**（Jest + React Testing Library）
2. **E2E 测试**（Playwright 或 Cypress）
3. **性能测试**（Lighthouse）
4. **视觉回归测试**（Percy）

---

**祝测试顺利！** 🎉
