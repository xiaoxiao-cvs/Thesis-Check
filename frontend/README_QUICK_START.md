# 论文检查系统前端 - 快速开始

## 📦 安装依赖

```bash
cd frontend
pnpm install

# 可选：如果需要论文预览功能
pnpm add mammoth
```

## 🚀 启动开发服务器

```bash
pnpm dev
```

访问：http://localhost:5173

## 🏗️ 项目结构

```
frontend/
├── src/
│   ├── api/              # API调用层
│   │   ├── request.js    # Axios配置
│   │   ├── auth.js       # 认证API
│   │   ├── papers.js     # 论文API
│   │   ├── check.js      # 检查API
│   │   ├── results.js    # 结果API
│   │   └── ...
│   │
│   ├── components/       # 可复用组件
│   │   ├── Layout/       # 主布局
│   │   ├── PrivateRoute/ # 路由守卫
│   │   ├── GradeTag/     # 成绩标签
│   │   ├── TableSkeleton/    # 表格骨架屏 ⭐新增
│   │   ├── CardSkeleton/     # 卡片骨架屏 ⭐新增
│   │   ├── StatisticSkeleton/ # 统计骨架屏 ⭐新增
│   │   └── PaperPreview/     # 论文预览 ⭐新增
│   │
│   ├── hooks/            # 自定义Hooks
│   │   ├── useAuth.js    # 认证Hook
│   │   ├── usePermission.js  # 权限Hook
│   │   ├── useWebSocket.js   # WebSocket Hook
│   │   ├── useLoading.js     # 加载状态Hook ⭐新增
│   │   └── useRequest.js     # 请求Hook（带重试）⭐新增
│   │
│   ├── pages/            # 页面组件
│   │   ├── Auth/         # 登录/注册
│   │   ├── Dashboard/    # 仪表盘
│   │   ├── Papers/       # 论文管理
│   │   ├── Check/        # 检查提交
│   │   ├── Results/      # 检查结果
│   │   ├── Templates/    # 模板管理
│   │   ├── PreviousPapers/ # 往届论文
│   │   ├── Parameters/   # 参数设置
│   │   ├── Statistics/   # 统计分析
│   │   ├── Users/        # 用户管理
│   │   ├── Profile/      # 个人中心
│   │   └── Error/        # 错误页面
│   │
│   ├── store/            # Redux状态管理
│   │   ├── index.js      # Store配置
│   │   └── authSlice.js  # 认证状态
│   │
│   ├── utils/            # 工具函数
│   │   ├── auth.js       # Token管理
│   │   ├── constants.js  # 常量定义
│   │   ├── format.js     # 格式化函数
│   │   ├── errorHandler.js  # 错误处理 ⭐新增
│   │   ├── validators.js    # 表单验证 ⭐新增
│   │   └── export.js        # 数据导出 ⭐新增
│   │
│   ├── styles/           # 全局样式
│   ├── App.jsx           # 应用入口
│   ├── router.jsx        # 路由配置
│   └── main.jsx          # 主入口
│
├── package.json
├── vite.config.js
├── FRONTEND_IMPROVEMENTS.md  # 优化文档 ⭐新增
└── README.md             # 本文件
```

## 🔑 核心功能

### 1. 用户认证
- [x] 登录/注册
- [x] JWT Token认证
- [x] 自动刷新Token
- [x] 权限路由守卫

### 2. 论文管理
- [x] 上传论文（毕业论文/课设论文）
- [x] 论文列表（搜索、筛选、分页）
- [x] 论文详情
- [x] 论文预览（Word文档在线预览）⭐新增
- [x] 批量删除 ⭐新增

### 3. 检查功能
- [x] 提交检查任务
- [x] 实时查看进度（WebSocket）
- [x] 检查结果列表
- [x] 结果详情（问题分类、统计）
- [x] 批量导出结果 ⭐新增

### 4. 管理功能
- [x] 模板管理（教师+）
- [x] 往届论文管理（教师+）
- [x] 参数设置（主任+）
- [x] 用户管理（管理员）
- [x] 统计分析（教师+）

### 5. 用户体验优化 ⭐新增
- [x] 骨架屏加载
- [x] 按钮防重复提交
- [x] 友好的错误提示
- [x] 网络错误自动重试
- [x] 表单验证增强

## 🎨 UI组件库

- **Ant Design 6.1.3** - 企业级UI组件库
- **ECharts** - 数据可视化图表
- **Less** - CSS预处理器

## 📱 响应式设计

支持桌面端和平板设备，移动端优化进行中。

## 🔐 权限等级

1. **学生** - 基础权限（上传论文、提交检查、查看结果）
2. **教师** - 查看所有学生论文、管理模板和往届论文
3. **主任** - 参数设置、统计分析
4. **院长** - 锁定/解锁参数
5. **管理员** - 用户管理

## 🚧 开发指南

### 添加新页面

1. 在 `src/pages/` 创建页面组件
2. 在 `src/router.jsx` 添加路由
3. 如需权限控制，使用 `<PrivateRoute>` 包裹

### 调用API

```jsx
import { getPaperList } from '@/api/papers';

const fetchPapers = async () => {
  try {
    const data = await getPaperList({ page: 1, page_size: 10 });
    console.log(data);
  } catch (error) {
    console.error(error);
  }
};
```

### 使用骨架屏

```jsx
import TableSkeleton from '@/components/TableSkeleton';

{loading ? (
  <TableSkeleton columns={5} rows={10} />
) : (
  <Table dataSource={data} columns={columns} />
)}
```

### 批量操作

```jsx
// 批量删除示例
const handleBatchDelete = async (ids) => {
  const promises = ids.map(id => deletePaper(id));
  const results = await Promise.allSettled(promises);
  
  const summary = handleBatchErrors(results);
  message.success(`成功删除 ${summary.success} 篇`);
};
```

## 📦 构建生产版本

```bash
pnpm build
```

构建产物在 `dist/` 目录

## 🧪 测试

```bash
# 运行单元测试（待实施）
pnpm test

# 运行E2E测试（待实施）
pnpm test:e2e
```

## 🌐 环境变量

创建 `.env.development` 和 `.env.production` 文件：

```env
# API基础URL
VITE_API_BASE_URL=http://localhost:8888/api/v1

# WebSocket URL
VITE_WS_URL=ws://localhost:8888/ws
```

## 📚 相关文档

- [Ant Design 文档](https://ant.design/)
- [React Router 文档](https://reactrouter.com/)
- [Vite 文档](https://vitejs.dev/)
- [优化实施报告](./FRONTEND_IMPROVEMENTS.md) ⭐推荐阅读

## 🐛 问题反馈

如有问题，请提交 Issue 或联系开发团队。

## 📄 许可证

MIT License
